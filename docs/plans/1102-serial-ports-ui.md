# Plan 1102: 端口枚举与参数接进界面

- **关联**：ROADMAP 阶段 11 ·「端口枚举与参数接进界面（列端口 + 手动路径兜底 + 池行取值 + 可读报错）」
- **前置**：plan 1101（会话能打开）· plan 0802（`ports()` 与 `TryFrom<u8>` 已就位）
- **状态**：未规划（骨架）
- **展开时机**：plan 1101 之后

## 目标

界面上能看到本机端口，挑一条或手动输入路径，并按池行的参数打开；
取值不合法时当场看到**字段与取值**。

## 非目标

- 会话的打开 / 关闭与回收（plan 1101）；设备消失（plan 1103）
- 热插拔事件驱动的列表刷新（先手动刷新）
- 串口配置池的增删改查界面（与主机池的界面缺口同一批，尚未规划）

## 判据（来自 ROADMAP，展开时必须变成可粘贴的验收命令）

界面上看到本机枚举结果并据此（或手输路径）打开；取值越界时显示字段与取值。

## 展开时要补

- [ ] 「## 前置检查」：⚠️ **枚举结果不代表可用**（问题 #150）—— 列表与"能不能打开"
      必须在呈现上分开，不得把列表当成"可用端口"
- [ ] 「## 步骤」：`PortInfo` / `PortKind` 过 IPC 的 DTO 落在哪一层
      （`akasha-serial` 不带 serde / specta，映射在 app 侧）
- [ ] 「## 验收命令」：本机枚举到 32 条（`/dev/ttyS*`，全部打不开）—— 判据用"手输 PTY 从端
      的路径"收口，不得依赖列表里有一条能用的
- [ ] 「## 验收命令」：生成物（`src/ipc/bindings.ts`）差异已提交，`gen-types-check` 覆盖
