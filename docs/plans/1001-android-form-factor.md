# Plan 1001: 评估 Android 上的托盘与端口转发形态

- **关联**：ROADMAP 阶段 10 ·「评估 Android 上的托盘与端口转发形态」
- **前置**：阶段 3（桌面托盘语义已定型）· 阶段 6（转发已定型）
- **状态**：未规划（骨架）
- **展开时机**：Android 立项时（当前 `later`）

## 目标

评估两件事在 Android 上的形态，并**写回 [`../scope.md`](../scope.md) §9 / §10**：

1. **托盘**：Android 没有系统托盘 —— 等价物（常驻通知 / 前台服务）与权限成本
2. **端口转发**：后台存活限制、`VpnService` 与本地监听的可行边界

## 非目标

- **不写任何 Android 代码**：v1 只保证 `Transport` 的 trait 边界能容纳它（`scope.md` §2）
- 不做 Android 打包 / CI（`cargo-ndk` 等工具链决策等立项时再说）

## 判据

评估结论有出处、有结论，**写进 `scope.md` 的对应章节**（不是只留在对话里）。

## 展开时要补

- [ ] 「## 步骤」：调研项清单（每个结论都要有可核对的出处）
- [ ] 「## 验收命令」：结论落到 `scope.md` 的哪一节 + `just docs-check` 绿
- [ ] 明确哪些结论会**反向影响桌面实现**（若会，先在本 plan 里标出来）
