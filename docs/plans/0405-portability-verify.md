# Plan 0405: 可搬迁性验证

- **关联**：ROADMAP 阶段 4 ·「可搬迁性验证」（展开见 [`../portable.md`](../portable.md)）
- **前置**：plan 0403（四套池的 CRUD —— 没有数据就验证不了"数据还在"）
- **状态**：未规划（骨架）
- **展开时机**：数据目录与 CRUD 都稳定之后

## 目标

把 [`../portable.md`](../portable.md) §5 的五步验证**做成可执行的配方**（当前只有方法，没有配方）：

1. 正常启动，确认数据落在 bin 同目录（而非 `~/.local/share` 之类）
2. 完全退出
3. 把整个文件夹移动到另一个路径
4. 从新位置启动
5. **断言原有主机 / 密钥 / 转发规则都在**

第 5 步是关键：**只验证"能开"不算过** —— 搬家后能开但规则全丢，正是"存了绝对路径"的典型症状。

## 非目标

- 不做 webview 及其依赖栈的环境重定向（`scope.md` §1.1 已豁免）
- 不做 `strace` 类全量写入审计（判据已简化为"搬走文件夹后还能开"，审计失去意义）

## 判据（来自 ROADMAP，展开时必须变成可粘贴的验收命令）

移动整个文件夹后重启，**原有主机 / 密钥 / 规则都在**。

## 展开时要补

- [ ] 「## 步骤」：配方形态（类似 `just syscheck`），哪几步自动、哪一步需人工触发
- [ ] 「## 验收命令」：`mv` → 启动 → 断言内容一致
- [ ] **新增配方必须同步 [`../just.md`](../just.md) §2**，否则 `just docs-check` 会红
- [ ] 便携模式下不可写时"启动即报错"这一条的验收形态（`portable.md` §4 第 3 条）
