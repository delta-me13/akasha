# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust 产物。根 workspace 之后 cargo 的 target 落在**仓库根**（ADR-0001 决策一），
# 原先写在 src-tauri/.gitignore 里的 /target/ 已随之作废。
/target/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 受限环境下运行 app 时的 XDG 重定向目录（见 docs/just.md）
.devhome/
# just ready 失败时落盘的完整输出
.just-ready-fail.log
